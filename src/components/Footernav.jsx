
const Footernav = ({ link }) => {
  return (
    <nav>
      <a className="link link-hover">{link}</a>
      <a className="link link-hover">{link}</a>
      <a className="link link-hover">{link}</a>
      <a className="link link-hover">{link}</a>
      <a className="link link-hover">{link}</a>
    </nav>
  );
};

export default Footernav;
